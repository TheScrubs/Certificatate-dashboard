export default function Certificate(id, title, lectures, reviews, subscribers, price, subject, level ) {
    this.id = id 
    this.title = title
    this.lectures = lectures 
    this.reviews = reviews
    this.subscribers = subject
    this.price = price
    this.subject = subject
    this.level = level
}